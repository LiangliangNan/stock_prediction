选定 Graph（图神经网络） 模型，预测5天、大样本回测 以及特定股票实战的完整执行流程。

请按顺序在终端（Terminal）中执行以下命令。

第一步：下载数据
```bash
  python scripts/getdata.py
````
第二步：数据预处理 (Data Preprocessing)
将原始 CSV 数据转化为 Graph 模型所需的二进制格式。
```bash
  python scripts/data_preprocess.py --pklname train.pkl
```
意义：当你运行 data_preprocess.py 或训练时，代码会自动为每一只股票计算大量的技术指标。
涵盖指标：MACD, KDJ, BOLL, CCI, DMI, TAQ, TRIX, ATR。
这一步会生成 pkl_handle/train.pkl，它是后续所有步骤的基础。

第三步：模型训练 (Training)
使用 graph 模型进行训练。关键点：必须在这里指定 --predict_days 5，否则模型输出维度不匹配。
```bash
  python scripts/train.py \
      --mode train \
      --model graph \
      --predict_days 5 \
      --epoch 50 \
      --pkl 1 \
      --pkl_queue 1 \
      --test_gpu 1
```
关键参数说明：
--model graph：启动图时空演化模型。
--predict_days 5：设定模型输出头为 5 天的序列。
--pkl_queue 1：在大样本训练时极大地提高 IO 效率。
生成文件：output/graph_out4_time30_pre5_Model.pkl。

第四步：大样本回测 (Batch Testing)
验证模型在未见过的大量样本上的泛化能力。
```bash
  python scripts/train.py --mode test --model graph --predict_days 5 --pkl 1 --test_gpu 1
```
运行模式与数据控制
--mode（str，默认值：train）。运行阶段：train（训练模型）、test（在历史数据上回测）、predict（对未来实际预测）。
--pkl（int，默认值：1）：数据开关：是否使用预处理好的 .pkl 文件。设为 1 时，代码会从 train_pkl_path 读取数据，速度极快；设为 0 则尝试从 CSV 实时加载和计算各种指标（特征工程）。
--pkl_queue（int，默认值：1）。加载加速：是否使用多进程队列加载数据。在大规模数据集上建议开启（1），能显著减少 GPU 等待数据的时间。
--model（模型架构选择）。代码支持多种主流模型，输入模型名会自动初始化对应的网络结构和损失函数：
  * 基础模型：LSTM, BILSTM, TCN (时间卷积网络)。
  * 进阶模型：TRANSFORMER, ATTENTION_LSTM (带注意力机制)。
  * 高级/自适应模型：
     - HYBRID (推荐)：混合模型，会自动调用 get_adaptive_hybrid_config 根据数据量大小自动调整模型深度（Tiny 到 Full）。
     - PTFT_VSSM：概率时空融合模型，适合处理金融数据的不确定性。
     - DIFFUSION / GRAPH：扩散模型或图神经网络分支。
--predict_days : 核心逻辑切换开关。 
     - >0：多步预测（Interval Prediction）。模型输出维度会包含预测天数，保存文件名会自动加上 _preN 标识。
     - <= 0：日期点预测或单步预测。
--trend: 如果设为 1，模型将不再预测具体价格，而是预测趋势（涨跌分类）。
--full_train: 如果设为 1，程序将不再划分验证集，而是使用全部可用数据进行训练。这通常用于模型最终上线前的最后一次调优。
--hybrid_..._weight（Hybrid 混合损失权重）。当使用 HYBRID 模型时，HybridLoss 会平衡多个目标。
   * 核心准度：mse_weight (价格偏差)。
   * 风险控制：volatility_weight (波动率惩罚)、extreme_weight (极端值抑制)。
   * 形态匹配：direction_weight (涨跌方向)、return_weight (收益率序列一致性)。

操作要领：不要指定 test_code。程序会自动读取 test_codes.txt，对其中所有的股票进行批量测试。
输出结果：
控制台会显示所有测试股票的平均 Mean MSE Loss。
png/ 目录下会生成大量 test_xxxxxx.png 图片，供你抽查模型在不同股票上的拟合度。

第五步：600879 专项实战预测 (Future Prediction)
使用专门的预测脚本对航天电子（600879）进行未来 5 天的递归预测。
```bash
  python scripts/predict.py --model graph --test_code "600879" --predict_days 5 --plot_days 30 --test_gpu 1
```
操作要领：
--test_code "600879"：锁定目标。
--plot_days 30：图表中会显示最近 30 天的历史走势作为背景，后面紧跟 5 天的预测红线。
输出结果：
会在 png/predict/ 下生成包含 600879 预测走势的图表。
会生成一个 metrics_600879_graph_时间戳.json，记录了具体的预测数值。


--------------------------------------------------------------------------------
现在模型已经训练好，以后每日实战三分钟流程：
--------------------------------------------------------------------------------
1. 每日实战三板斧（只需执行这两步）
当你的 graph_..._Model.pkl 已经躺在 output 文件夹里时，你每天只需要做：
  第一步：更新当日数据 (getdata.py)
         交易日收盘后（通常 15:30 以后），运行脚本获取当天的最新 K 线，存入 daily_path。
          ```bash
              python scripts/getdata.py
          ```
  第二步：直接实战预测 (predict.py)
         直接运行预测脚本。它会自动加载已经训练好的模型和归一化参数，读取刚下载的最新数据，推算未来 5 天。
          ```bash
              python scripts/predict.py --model graph --test_code "600879" --predict_days 5
          ```

--------------------------------------------------------------------------------
自动化建议：一键实战脚本
--------------------------------------------------------------------------------
你可以写一个 daily_trade.sh 脚本放在桌面，每天收盘点一下：
  ```bash
      #!/bin/bash
      # 1. 下载最新数据
      python scripts/getdata.py

      # 2. 预测你关注的股票池 (假设你有三只)
      stocks=("600879" "600519" "000001")
      
      for code in "${stocks[@]}"
      do
          echo "正在预测 $code ..."
          python scripts/predict.py --model graph --test_code "$code" --predict_days 5 --plot_days 30
      done
      
      echo "预测完成，请去 png/predict 文件夹查看结果。"
  ```




--------------------------------------------------------------------------------
Train-test split:
--------------------------------------------------------------------------------
在 train.py 的 1615 行（或附近）：
  - train_size = int(TRAIN_WEIGHT * (data.shape[0]))
  - Train_data=data[:train_size+SEQ_LEN]
如果 TRAIN_WEIGHT = 0.8，意味着前 80% 的日期用于训练，最后 20% 用于回测。
想要回测时间更长？ 在 init.py 的开头处调小 TRAIN_WEIGHT（如 0.7）
模型并不是直接看日期，而是看窗口。如果你预测 5 天，模型会拿 Train_data 最后的 30 天（SEQ_LEN）去预测接下来的 5 天。

如何精准设置回测日期？（
由于代码是按数据行数比例切分的，而不是按具体日期字符串切分的，如果你想指定“从 2024 年 1 月开始回测”，有以下两种方法：
  - 方法一：通过原始 CSV 控制（最简单）
    如果你只想回测 2024 年的数据：
    在运行 data_preprocess.py 之前，确保你的 daily_path 文件夹下的 CSV 文件只包含到 2024 年底的数据。
    调整 TRAIN_WEIGHT，使得 1 - TRAIN_WEIGHT 的数据量正好覆盖 2024 年的交易天数。
  - 方法二：修改 load data 的逻辑
  进入 src/stock_prediction/common.py，找到 import_csv 函数，按下图所示位置添加代码：
  ```python
      def import_csv(stock_code, dataFrame=None, csv_file=None):
          try:
              if dataFrame is None:
                  # ... 原有的读取逻辑 ...
                  if os.path.exists(file_path):
                      df = pd.read_csv(file_path)
                  else:
                      return None
              # ... 省略部分代码 ...
              
              # --- 在这里插入你的日期过滤逻辑 ---
              # 确保日期列格式正确，然后过滤
              if 'trade_date' in df.columns:
                  df = df[df['trade_date'].astype(str) >= '20180101']
              elif 'Date' in df.columns:
                  # 如果 CSV 里的 Date 是 2018-01-01 这种格式，需要匹配格式
                  df['Date'] = pd.to_datetime(df['Date'])
                  df = df[df['Date'] >= '2018-01-01']
              # --------------------------------
              
              if df.empty: return None # 如果过滤后没数据了，直接跳过
              
              result = add_target(df) # 这一步会把过滤后的数据拿去算指标
              # ... 后续逻辑 ...
  ```
